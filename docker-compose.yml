services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gameapi
      POSTGRES_PASSWORD: gameapi
      POSTGRES_DB: gameapi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
